#ifndef NODE_H
#define NODE_H

#include <stdio.h>

class Node {
public:
  Node(int v = 0, Node *n = nullptr);
  void SetValue(int v);
  void SetNext(Node *n);
  int GetValue() const;
  Node *GetNext() const;
  void PrintNode();

private:
  int value;
  Node *nextNode;
};
#endif
